#include <string>
#include <vector>
#include<iostream>
#include <memory>


using namespace std;

//문자열 특징 : 맨 뒷 비트가 0으로 끝나야 문자열이 끝났다고 판단한다.
unsigned __int64 Length(const char* str) {

	return 
}
bool Compare(const char* str1, const char* str2) {
	return false;
}

int main() {
	const char* str1 = "Hello";

	cout << Length(str1);
	
	return 0;
}